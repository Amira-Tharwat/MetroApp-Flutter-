import 'package:flutter/material.dart';
import 'package:sekaa/SplashScreen.dart';

void main() {
  runApp(
    MaterialApp(
      debugShowCheckedModeBanner: false,
      home: SplashScreen(),
    ), // Added a comma here and closed the parentheses
  ); // Closed the runApp function
}
